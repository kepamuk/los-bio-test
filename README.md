# LOS-BIO - React Application

Современное одностраничное приложение (SPA) для компании LOS-BIO, специализирующейся на биотехнологиях. Приложение построено с использованием лучших практик React и современного стека технологий.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск приложения в режиме разработки

```bash
npm run dev
```

Приложение автоматически откроется по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для production

```bash
npm run build
```

Production build создается в директории `dist/` и готов к деплою на любой статический хостинг.

### Предпросмотр production сборки

```bash
npm run preview
```

Эта команда запускает локальный сервер для тестирования production сборки.

### Запуск тестов

```bash
# Запуск всех тестов
npm test

# Запуск тестов с UI-интерфейсом
npm run test:ui

# Запуск тестов с покрытием кода
npm run test:coverage
```

### Docker (опционально)

#### Production сборка с Docker

```bash
# Собрать и запустить production контейнер
docker-compose up app

# Или напрямую через Docker
docker build -t los-bio .
docker run -p 80:80 los-bio
```

Приложение будет доступно по адресу [http://localhost](http://localhost)

#### Development режим с Docker

```bash
# Запустить development контейнер с hot reload
docker-compose up dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

## 📋 Описание проекта

LOS-BIO - это современное веб-приложение для презентации биотехнологических проектов и услуг компании. Приложение предоставляет интуитивно понятный интерфейс для просмотра информации о компании, её преимуществах и портфолио проектов.

### Основные возможности:

- 🎯 **Главная страница** с интерактивным слайдером
- ⭐ **Раздел преимуществ** компании
- 📁 **Каталог проектов** с детальными страницами
- 📱 **Адаптивный дизайн** для всех устройств
- ⚡ **Быстрая загрузка** благодаря code splitting
- 🔄 **Кэширование данных** для оптимальной производительности

## 🛠 Технологический стек

### Основные технологии

- **React 18.3.1** - Библиотека для построения пользовательских интерфейсов
- **Vite 5.4.3** - Современный инструмент сборки
- **React Router v6.26.1** - Декларативная маршрутизация для React

### Управление состоянием

- **Redux Toolkit 2.2.7** - Официальный инструментарий для эффективного управления состоянием Redux
- **RTK Query** - Мощный инструмент для получения и кэширования данных

### Стилизация

- **Styled Components 6.1.13** - CSS-in-JS решение для компонентной стилизации

### UI-компоненты

- **Swiper 11.1.14** - Современная библиотека для создания слайдеров

### Тестирование

- **Vitest** - Современный и быстрый test runner для Vite
- **Testing Library** - Утилиты для тестирования React компонентов
- **@testing-library/jest-dom** - Кастомные matchers для DOM
- **jsdom** - JavaScript реализация веб-стандартов

### Контейнеризация

- **Docker** - Контейнеризация для development и production
- **docker-compose** - Оркестрация контейнеров
- **nginx** - Веб-сервер для production сборки

## 🏗 Архитектура проекта

```
src/
├── api/                    # API слой
│   ├── apiSlice.ts        # RTK Query endpoints
│   └── apiSlice.test.ts   # Тесты API
├── app/                   # Конфигурация приложения
│   └── store.ts           # Redux store
├── assets/                # Статические файлы
│   ├── icons/            # SVG иконки
│   └── images/           # Изображения
├── components/            # Переиспользуемые компоненты
│   ├── Home/             # Компоненты главной страницы
│   │   ├── HeroSlider.tsx
│   │   ├── Advantages.tsx
│   │   └── Projects.tsx
│   ├── Layout/           # Компоненты макета
│   │   ├── Container.tsx
│   │   ├── Container.test.tsx
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   └── Layout.tsx
│   └── UI/               # UI-компоненты
│       ├── Loading.tsx
│       ├── Loading.test.tsx
│       ├── ErrorMessage.tsx
│       └── ErrorMessage.test.tsx
├── hooks/                 # Кастомные хуки
│   ├── useMediaQuery.ts
│   └── useMediaQuery.test.ts
├── pages/                 # Страницы приложения
│   ├── HomePage.tsx
│   └── ProjectPage.tsx
├── styles/                # Стили и темы
│   ├── GlobalStyles.ts
│   └── theme.ts
├── types/                 # TypeScript типы
│   ├── api.ts
│   ├── index.ts
│   └── styled.d.ts
├── utils/                 # Утилиты
│   └── constants.ts
├── setupTests.ts          # Конфигурация тестов
├── App.tsx                # Корневой компонент
└── main.tsx              # Точка входа
```

## 🎯 Ключевые архитектурные решения

### 1. Redux Toolkit + RTK Query

**Обоснование:**
- **Упрощенная настройка**: RTK значительно упрощает конфигурацию Redux
- **Встроенное кэширование**: RTK Query автоматически кэширует данные и управляет состоянием загрузки
- **Оптимизация запросов**: Дедупликация одинаковых запросов
- **DevTools**: Отличная интеграция с Redux DevTools для отладки

**Альтернативы и почему они не выбраны:**
- React Query: Хороший инструмент, но Redux Toolkit предоставляет унифицированное решение для всего состояния
- Context API: Не подходит для сложного состояния и не имеет встроенного кэширования
- Zustand: Более легковесный, но менее подходит для энтерпрайз-приложений

### 2. Styled Components

**Обоснование:**
- **Изоляция стилей**: Каждый компонент имеет свои стили, нет глобальных конфликтов
- **Динамические стили**: Легко передавать props для изменения стилей
- **Темизация**: Встроенная поддержка тем через ThemeProvider
- **TypeScript ready**: Отличная поддержка типизации (при необходимости)

**Альтернативы:**
- CSS Modules: Требует дополнительной конфигурации
- Tailwind CSS: Менее гибок для точной пиксель-в-пиксель вёрстки по макетам
- Emotion: Аналогичен Styled Components, но Styled Components более популярен

### 3. React Router v6

**Обоснование:**
- **Декларативный API**: Простота и читаемость кода
- **Code Splitting**: Легкая интеграция с React.lazy()
- **Hooks**: useParams, useNavigate и другие хуки упрощают работу
- **Стандарт индустрии**: Наиболее популярное решение для роутинга в React

### 4. Vite

**Обоснование:**
- **Скорость**: Мгновенный запуск dev-сервера
- **HMR**: Быстрая горячая замена модулей
- **Оптимизированная сборка**: Использует Rollup для production
- **Современные стандарты**: Нативная поддержка ES-модулей

### 5. SVG как React-компоненты

**Обоснование:**
- **vite-plugin-svgr**: Плагин для импорта SVG как React-компонентов
- **Типизация**: Полная поддержка TypeScript
- **Оптимизация**: Автоматическая оптимизация SVG
- **Гибкость**: Возможность стилизации через props

**Использование:**
```typescript
import LogoIcon from './assets/icons/logo.svg?react';
<LogoIcon width={40} height={40} />
```

### 6. Vitest для тестирования

**Обоснование:**
- **Интеграция с Vite**: Использует ту же конфигурацию
- **Скорость**: Быстрое выполнение тестов
- **Совместимость**: API совместимо с Jest
- **Современность**: Поддержка ESM из коробки

**Альтернативы:**
- Jest: Требует дополнительной настройки для Vite и TypeScript
- Mocha: Менее удобный API для React-компонентов

### 7. Docker для контейнеризации

**Обоснование:**
- **Multi-stage build**: Минимальный размер production образа
- **nginx**: Эффективная раздача статики
- **Изоляция**: Консистентное окружение на всех стадиях
- **docker-compose**: Удобное управление development/production

## 📱 Адаптивность

Приложение адаптировано под следующие брейкпоинты:

- **Mobile**: >= 375px
- **Tablet**: >= 768px
- **Small Desktop**: >= 991px
- **Desktop**: >= 1200px
- **Extra Small**: < 375px (масштабируется)

Реализация через:
- CSS Media Queries в Styled Components
- Кастомные хуки `useMediaQuery`, `useIsMobile`, `useIsTablet`, etc.

## 🎨 Работа с дизайном

Дизайн реализован по макетам Figma с точностью до пикселя. Все отступы, размеры шрифтов и цвета соответствуют макетам.

### Структура темы:

```javascript
theme = {
  breakpoints,  // Брейкпоинты для адаптивности
  colors,       // Цветовая палитра
  spacing,      // Система отступов
  fontSizes,    // Размеры шрифтов
  shadows,      // Тени
  borderRadius, // Радиусы скругления
  transitions   // Анимации
}
```

## 🚀 Оптимизация производительности

### 1. Code Splitting
- `ProjectPage` загружается динамически через `React.lazy()`
- Снижает размер начального бандла

### 2. Lazy Loading
- Изображения загружаются с атрибутом `loading="lazy"`
- Первый слайд загружается с `loading="eager"`

### 3. RTK Query кэширование
- Автоматическое кэширование ответов API
- Предотвращение дублирующих запросов

### 4. React.StrictMode
- Включен для выявления потенциальных проблем

## 🔌 API Endpoints

Базовый URL: `https://api.los-bio.ru`

### Эндпоинты:

- `GET /info/group/slide` - Получение слайдов
- `GET /info/group/advantage` - Получение преимуществ
- `GET /projects/` - Получение списка проектов
- `GET /projects/:slug` - Получение конкретного проекта

## 🧪 Тестирование

Проект включает комплексное покрытие unit-тестами:

### Тестируемые компоненты:
- **Loading** - проверка различных состояний загрузки
- **ErrorMessage** - тестирование обработки ошибок и retry функционала
- **Container** - проверка layout стилей

### Тестируемые хуки:
- **useMediaQuery** - тестирование media queries
- **useIsMobile, useIsTablet, useIsDesktop** - тестирование breakpoints

### Тестируемые API:
- **apiSlice** - проверка endpoints и их конфигурации

### Запуск тестов:

```bash
# Однократный запуск
npm test

# Watch mode
npm test -- --watch

# С UI интерфейсом
npm run test:ui

# С отчетом о покрытии
npm run test:coverage
```

## 🧪 Обработка ошибок

Приложение включает комплексную обработку ошибок:

- Компонент `Loading` для состояний загрузки
- Компонент `ErrorMessage` для отображения ошибок
- Кнопка "Попробовать снова" для повторных запросов
- Отдельная обработка 404 ошибок для несуществующих проектов

## 📦 Структура данных

### Слайд (Slide)
```javascript
{
  id: number,
  title: string,
  description: string,
  image: string
}
```

### Преимущество (Advantage)
```javascript
{
  id: number,
  title: string,
  description: string,
  icon: string
}
```

### Проект (Project)
```javascript
{
  id: number,
  slug: string,
  title: string,
  description: string,
  content: string,
  image: string,
  date: string,
  category: string,
  author: string
}
```

## 🎯 Best Practices

### React
- ✅ Функциональные компоненты с хуками
- ✅ Разделение компонентов по ответственности
- ✅ Использование React.memo для оптимизации (где необходимо)
- ✅ Правильное именование компонентов (PascalCase)
- ✅ TypeScript для типобезопасности

### Стилизация
- ✅ Использование темизации для согласованности
- ✅ Отзывчивые компоненты с media queries
- ✅ Использование переменных для повторяющихся значений
- ✅ Избегание глобальных стилей (только GlobalStyles для reset)

### Производительность
- ✅ Code splitting для крупных компонентов
- ✅ Lazy loading изображений
- ✅ Оптимизация рендеринга через RTK Query
- ✅ nginx с gzip сжатием в production

### Структура кода
- ✅ Модульная архитектура
- ✅ Разделение логики и представления
- ✅ Переиспользуемые компоненты
- ✅ Unit-тесты для критических компонентов

### DevOps
- ✅ Docker для консистентных сборок
- ✅ Multi-stage builds для минимизации размера образа
- ✅ Отдельные конфигурации для dev/prod
- ✅ Health checks для production контейнеров

## 🔄 State Management Flow

```
Component → Hook (useGetDataQuery)
    ↓
RTK Query → API Request
    ↓
Redux Store → Cache
    ↓
Component Update (re-render)
```

## 🐳 Docker

### Production деплой

```bash
# Сборка образа
docker build -t los-bio:latest .

# Запуск контейнера
docker run -d -p 80:80 --name los-bio los-bio:latest

# Или через docker-compose
docker-compose up -d app
```

### Development окружение

```bash
# Запуск с hot reload
docker-compose up dev

# Остановка
docker-compose down
```

### Особенности Docker конфигурации:

- **Multi-stage build** для минимизации размера образа
- **nginx Alpine** (~23MB) для production
- **Кэширование слоев** для быстрой пересборки
- **Health checks** для мониторинга
- **Security headers** в nginx конфигурации
- **Gzip сжатие** для оптимизации трафика
- **SPA routing** через nginx try_files

## 📝 Дополнительные заметки

### Требования к окружению

**Локальная разработка:**
- Node.js >= 18.0.0
- npm >= 9.0.0

**Docker (опционально):**
- Docker >= 20.10
- docker-compose >= 2.0

### Совместимость браузеров
- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)

### Реализованные улучшения
- ✅ Unit-тесты (Vitest + Testing Library)
- ✅ TypeScript для типобезопасности
- ✅ Docker для контейнеризации
- ✅ Code splitting и lazy loading
- ✅ RTK Query для кэширования
- ✅ SVG как React-компоненты
- ✅ Адаптивный дизайн

### Дальнейшие улучшения
- SEO оптимизация (мета-теги, Open Graph)
- PWA функциональность (Service Workers)
- E2E тесты (Playwright/Cypress)
- Анимации страниц при переходах (Framer Motion)
- Фильтрация и поиск проектов
- Интернационализация (i18n)
- Аналитика (Google Analytics / Яндекс.Метрика)
